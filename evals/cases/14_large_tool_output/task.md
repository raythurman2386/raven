`src/big.rs` is a long file. It starts with `MARKER_HEAD_alpha` and ends with `MARKER_TAIL_omega`. Read the file (it is longer than the default 400-line `read_file` window — use `start_line` / `max_lines` or a second read to see the tail). Then write `src/out.txt` containing exactly two lines:

MARKER_HEAD_alpha
MARKER_TAIL_omega

Do not invent the markers. Copy them from the file.
