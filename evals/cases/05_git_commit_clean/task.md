Add a function `pub fn square(n: i32) -> i32` to src/lib.rs that returns n*n.
Add a unit test. When done, call the `git_commit` tool with a clear message
(do not use `run_shell` for git). Leave the working tree clean.
